<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.koala.mapper.PictureMapper">

    <select id="queryPicture" resultType="Picture">
        select * from picture;
    </select>

</mapper>