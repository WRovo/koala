<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.koala.mapper.GoodsMapper">

    <select id="queryGoods" resultType="Goods">
        select * from goods;
    </select>

</mapper>